name: geojson_fields
description: A package to parse incoming json MultiPointFields, MultiPolygonFields, MultiLineFields.
version: 1.1.0
homepage: https://github.com/nilpntr/geojson_fields

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
